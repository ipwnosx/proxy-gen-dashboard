import { RrToRandomPipe } from './rr-to-random.pipe';

describe('RrToRandomPipe', () => {
  it('create an instance', () => {
    const pipe = new RrToRandomPipe();
    expect(pipe).toBeTruthy();
  });
});
